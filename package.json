{
  "name": "oauth-examples",
  "version": "1.0.11",
  "description": "",
  "private": true,
  "workspaces": [
    "OAuth-Examples-Client",
    "OAuth-Examples-API"
  ],
  "scripts": {
    "client": "NODE_ENV=development yarn workspace OAuth-Examples-Client serve",
    "client-test": "yarn workspace OAuth-Examples-Client test",
    "server": "NODE_ENV=development yarn workspace OAuth-Examples-API start",
    "server-test": "yarn workspace OAuth-Examples-API test",
    "start": "concurrently --kill-others-on-fail \"yarn server\"  \"yarn client\"",
    "test": "concurrently --kill-others-on-fail \"yarn server-test\"  \"yarn client-test\""
  },
  "repository": {
    "type": "git"
  },
  "keywords": [],
  "author": "JT Houk <jt1992@gmail.com> (https://jt.houk.space/)",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^5.0.0"
  }
}
