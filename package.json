{
  "name": "oauth-examples",
  "version": "1.0.13",
  "description": "",
  "private": true,
  "workspaces": [
    "OAuth-Examples-Client",
    "OAuth-Examples-API"
  ],
  "scripts": {
    "client": "NODE_ENV=development yarn workspace oauth-client serve",
    "client-test": "yarn workspace oauth-client test",
    "client-deploy": "yarn workspace oauth-client deploy",
    "server": "NODE_ENV=development yarn workspace oauth-api start",
    "server-test": "yarn workspace oauth-api test",
    "server-deploy": "yarn workspace oauth-api deploy",
    "start": "concurrently --kill-others-on-fail \"yarn server\"  \"yarn client\"",
    "test": "concurrently \"yarn server-test\"  \"yarn client-test\"",
    "deploy": "concurrently \"yarn server-deploy\"  \"yarn client-deploy\""
  },
  "repository": {
    "type": "git"
  },
  "keywords": [],
  "author": "JT Houk <jt1992@gmail.com> (https://jt.houk.space/)",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^5.0.0"
  }
}
